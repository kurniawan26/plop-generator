import { ActionType } from "./action";

function {{properCase name}}Reducer({{camelCase name}} = null, action = {}) {
  switch (action.type) {
    case ActionType.SET_YOUR_ACTION:
      return action.payload;
    default:
  }
}

export default {{properCase name}}Reducer;
